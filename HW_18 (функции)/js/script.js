'use strict'

const arr = [1, 2, 3, -1, -2, -3];

function even (arr){

    let evenArr= []

    if (evenArr.length === 0 ){
        return 'Массив пустой'
    }

    for (let i = 0; i < arr.length ;i++){
        
        if ((arr[i] % 2) === 0){

            evenArr.push(currentPositiveNumber);

        }

        return console.log(evenArr);
  	    
    }

    
}

